# Task S3-6.3-E

`AUTO_CONTINUE: yes`

## Goal

用聚焦单元测试锁定第10集边界：即使 QC-approved memory 仍有未解决问题，后端也不生成第11集义务或义务证据。

## Allowed Files

- `tests/test_qc_grounding.py`
- `tasks/execution_log.md`（仅追加）
- `app/services/qc_grounding.py`（仅测试揭示缺口时允许最小修改；一旦修改，完成本任务后必须停止批次）

## Requirements

1. 使用 `valid_qc_pass_report_data(episode_number=10)`，确认报告包含未解决问题且没有义务。
2. 调用 `complete_missing_continuity_obligations()`。
3. 断言 `continuity_obligations` 仍为空，且不存在以 `continuity_obligations.` 开头的证据路径。
4. 不增加第11集 Schema、特殊 API 或 Prompt 逻辑。

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest -q tests/test_qc_grounding.py
.\.venv\Scripts\python.exe -m pytest -q tests/test_scripts.py -k "showrunner_qc"
git diff --check
```

按模板追加 `S3-6.3-E/attempt-N`。若修改了生产代码，即使测试通过也停止批次。
