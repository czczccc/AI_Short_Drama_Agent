# Task S3-7B-A3-R2

`AUTO_CONTINUE: no`

## Goal

补齐 QC Prompt 中 `carried_forward` 的完整输出契约，使模型知道“标记继续带到下一集”时必须把同一 obligation ID 写回本集正式 Memory，并提供本集场景证据；不修改后端校验或放宽门禁。

## Allowed Files

- `app/prompts/qc_v1.md`
- `tests/test_qc_prompt.py`
- `tasks/execution_log.md`（仅追加）

## Requirements

1. 明确第 1–9 集中，`continuity_resolutions.N.status=carried_forward` 时：
   - 同一 `obligation_id` 必须出现在 `approved_memory.continuity_obligations`。
   - 新义务的 `source_episode_number` 为当前集，`due_episode_number` 为下一集。
   - `source_memory_path` 必须指向本集 `approved_memory` 中真实存在且由场景支持的路径，不能沿用上一集路径。
   - 对应 `continuity_obligations.N` 必须有本集 `memory_evidence`，且逐字复制 evidence catalog。
2. `resolved` 的事项不得再次写入本集 obligations；第 10 集不得使用 carried_forward。
3. 在 Prompt 中加入一个最小非空 carried-forward JSON 示例或等价的完整字段示例，避免只写抽象文字。
4. 新增 Prompt 契约测试，断言上述关键约束和示例存在。
5. 不修改 Schema、Service、Provider、API 或数据库逻辑。

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest -q tests/test_qc_prompt.py tests/test_qc_grounding.py
.\.venv\Scripts\python.exe -m pytest -q
git diff --check
```

完成后停止等待 Codex；不得真实复测第 4 集或执行第 5 集。

