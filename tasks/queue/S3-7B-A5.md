# Task S3-7B-A5

`AUTO_CONTINUE: no`

## Goal

只读完成项目 16 第 1–5 集第一次连续性检查点，不进行任何新生成。

## Allowed Files

- `tasks/execution_log.md`（仅追加）

## Requirements

1. 第 1–5 集 Script、QC pass 和 qc_approved Memory 必须全部存在，QC approved_memory 必须逐集等于正式 Memory。
2. 第 1–4 集每条下一集到期义务必须在下一集 Brief 的 continuity_contract 中按稳定 obligation ID 出现，并在下一集 QC continuity_resolutions 中有完成状态和有效场景证据。
3. 第 1–5 集 Memory episode_number 连续，后一集不得覆盖前集；记录每集稳定 SHA-256、义务数量、解决数量和末场地点/时间，不记录正文。
4. 核对本批全部 Request ID 的 HTTP、LLM、Writer、QC 日志链和尝试上限；确认无隐藏重试。
5. 运行全部 pytest，确认真实数据写入不影响自动测试。

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest -q
git diff --check
git diff --name-only -- app tests requirements.txt
```

记录 `accepted` 或精确阻塞集/义务 ID 后停止批次，等待 Codex 验收；不得开始第 6 集。

