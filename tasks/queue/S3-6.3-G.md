# Task S3-6.3-G

`AUTO_CONTINUE: no`

## Goal

完成 Phase S3-6.3 的文档和全量自动测试证据，但不自行关闭 Phase 或更新任务状态。

## Required Context

按 AGENTS.md 规则只读取：

- `docs/01_architecture/API.md`
- `docs/01_architecture/DATA_MODEL.md`
- `docs/02_ai/WORKFLOW.md`
- `app/services/qc_grounding.py` 中补全函数

## Allowed Files

- `docs/01_architecture/API.md`
- `docs/01_architecture/DATA_MODEL.md`
- `docs/02_ai/WORKFLOW.md`
- `tasks/execution_log.md`（仅追加）

## Requirements

1. 文档准确说明第1–9集缺失义务由后端按精确来源路径补全并复用证据，第10集不生成下一集义务。
2. 明确该流程不新增 LLM 调用、不改变 API 请求/响应结构，QC 通过前仍不写正式 Story Memory。
3. API 文档只记录现有剧本生成接口行为，不新增不存在的端点。
4. 运行全部 pytest；失败时停止，不得为通过全量测试顺手修改其他代码。

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest -q
git diff --check
```

按模板追加 `S3-6.3-G/attempt-N`。完成后停止批次，等待 Codex整体验收；不得修改 TASKS 状态或开始 S3-7A。
