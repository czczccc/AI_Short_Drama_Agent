# Task S3-7B-A3-R4

`AUTO_CONTINUE: no`

## Goal

让 QCAgent 的一次 grounding 语义重答不仅收到机器错误对象，还收到由后端确定性生成的、包含具体 obligation ID 和 memory path 的修正指令；不改变校验标准、不自动修补正式 Memory。

## Allowed Files

- `app/agents/qc.py`
- `tests/test_showrunner.py` 或现有最贴近 QCAgent 重答的单个测试文件
- `tasks/execution_log.md`（仅追加）

## Requirements

1. 新增小型纯函数或私有方法，把现有安全 `issues` 转换为 `correction_instructions`；至少覆盖：
   - `carried_forward_obligation_not_saved`：带出具体 obligation ID，并明确同 ID 写入本集 obligations、当前/下一集号、本集来源路径和本集证据。
   - `missing_memory_evidence`：带出所有具体 `memory_paths`，要求逐条从 evidence catalog 复制证据。
   - `invalid_continuity_obligation_source`：带出具体 obligation ID，要求改成本集正式 Memory 的实际来源路径。
2. 第二次 user prompt 同时保留原始输入、结构化 `context_issues` 和新的 `correction_instructions`，要求输出完整 JSON。
3. 指令只能使用 validator 已提供的安全 ID/路径，不包含完整模型输出，不猜测场号、证据原文或剧情事实。
4. 不修改 `qc_v1.md`、Schema、grounding validator、Service、Provider、API 或数据库。
5. 测试必须捕获第二次发给 Provider 的 user prompt，并证明具体 obligation ID、缺失 memory path 和对应动作同时存在；不能只测试静态字符串。

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest -q tests/test_showrunner.py
.\.venv\Scripts\python.exe -m pytest -q
git diff --check
```

完成后停止等待 Codex；不得调用真实 LLM 或执行 R5/第 5 集。

