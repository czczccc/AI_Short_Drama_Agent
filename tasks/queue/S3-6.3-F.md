# Task S3-6.3-F

`AUTO_CONTINUE: yes`

## Goal

执行只读范围与回归门禁，证明本批次没有引入新 LLM 调用、模糊匹配、API/Schema 改动或生产代码变更。

## Allowed Files

- `tasks/execution_log.md`（仅追加）

不得修改测试或生产代码。

## Requirements

1. 用 `git diff --name-only -- app tests requirements.txt` 确认本批次在生产/测试范围内只修改 `tests/test_qc_grounding.py`；如出现 `app/`、其他测试、Schema、Prompt 或依赖文件，停止批次。任务治理文档的既有未提交改动不计入此检查。
2. 只读检查补全函数仍按精确 `source_memory_path` 映射，没有模糊文本逻辑或新 Provider 调用。
3. 运行 QC grounding、连续性合同、QC Schema 和 Showrunner QC 定向回归。

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest -q tests/test_qc_grounding.py tests/test_continuity_contract.py tests/test_qc_schema.py
.\.venv\Scripts\python.exe -m pytest -q tests/test_scripts.py -k "showrunner_qc or continuity"
git diff --check
```

按模板追加 `S3-6.3-F/attempt-N`，记录只读检查证据。任何范围异常都返回 blocked。
