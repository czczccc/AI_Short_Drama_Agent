# Task S3-7B-A2-R3

`AUTO_CONTINUE: yes`

## Goal

在 R2 prompt 修复后，重跑项目 16 第 3 集真实生成：重生成 Writer Brief（force_regenerate）后发送唯一一次 script 请求，审计通过则继续 A3；失败则停止批次。

## Authorization And Flow

- 授权真实 DeepSeek 调用与现有 API 对 `data/app.db`、`logs/app.jsonl` 的正常写入。
- 因现有 EP3 Brief 由旧 prompt 生成且含冲突措辞，先调用一次 `POST /api/v1/projects/16/episodes/3/writer-brief`（body 含 `force_regenerate: true`）。
- Brief 成功后，只调用一次第 3 集 script API（`use_showrunner_brief=true`、`run_showrunner_qc=true`、`max_revision_attempts=2`）。
- 每个请求使用唯一 `X-Request-ID`，调用前确认日志中不存在；不因非 200 重发。

## Allowed Files

- `data/app.db`、`logs/app.jsonl`（仅由现有 API 写入）
- `tasks/execution_log.md`（仅追加）

## Acceptance Gate

- script 请求 HTTP 200 且 QC pass：Script 与 `qc_approved` Memory 原子保存、QC approved_memory == 正式 Memory、Brief 未被覆盖、EP1–2 未变化 → 继续 A3。
- 非 200（409/502/503）或原子性异常：记录状态、日志阶段与原子性结果后停止批次；不修改代码或 Prompt。
- 3 次 Writer 尝试（1 初始 + 2 返修）后仍 fail 属预期业务拒绝，不得无限重试。

## Verification

```powershell
git diff --check
git diff --name-only -- app tests requirements.txt
```

按模板追加 `S3-7B-A2-R3/attempt-N`。
