# Task S3-7B-A2-R2

`AUTO_CONTINUE: yes`

## Goal

修复 Writer Brief 生成 prompt 中"角色暗示掌握信息"措辞与 `forbidden_content` 的冲突，消除方琳类结构性越界诱导；不改 Schema、不改 QC、不弱化门禁。

## Allowed Files

- `app/prompts/showrunner/brief_v1.py`
- `tests/test_showrunner.py` 或 `tests/test_showrunner_schema.py`
- `tasks/execution_log.md`（仅追加）

## Requirements

1. 在 `brief_v1.py` 的 SYSTEM_PROMPT 中增加一致性要求：当 `allowed_scope` / `required_beats` / `ending_requirement` 需要角色"暗示掌握信息或可能提供帮助"时，暗示只能停留在"存在未言明的帮助或办法"，不得落到 `forbidden_content` 明确禁止的具体事实（如保留备份、参与方式、证据细节、身份关系）；不得生成让 Writer 无法同时满足的冲突指令。
2. 不改 WriterBrief Schema、QC prompt、Writer prompt、服务代码。
3. 新增一个测试，断言 prompt 包含该一致性自检要求（如关键短语）；运行相关测试与全量 pytest。

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest -q tests/test_showrunner.py tests/test_showrunner_schema.py
.\.venv\Scripts\python.exe -m pytest -q
git diff --check
```

按模板追加 `S3-7B-A2-R2/attempt-N`。本任务只改 prompt 与测试，不调用 LLM。
