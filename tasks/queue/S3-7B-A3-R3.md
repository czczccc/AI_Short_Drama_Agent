# Task S3-7B-A3-R3

`AUTO_CONTINUE: no`

## Goal

在 carried-forward QC Prompt 契约修复后，对项目 16 第 4 集执行恰好一次真实 script 复测，并审计正式数据原子性与历史稳定性。

## Authorization And Flow

- 授权当前配置的真实 DeepSeek 调用，以及现有 API 对 SQLite/JSONL 的正常写入。
- 复用现有第 4 集 Writer Brief；本次修复只影响 QC Prompt，不重生成 Brief。
- 只调用一次 `POST /api/v1/projects/16/episodes/4/script`，请求体使用 `target_duration_seconds=90`、`use_showrunner_brief=true`、`run_showrunner_qc=true`、`max_revision_attempts=2`。
- 使用唯一 `X-Request-ID`，调用前确认日志不存在；不因任何状态码重发。

## Allowed Files

- `data/app.db`、`logs/app.jsonl`（仅由现有 API 写入）
- `tasks/execution_log.md`（仅追加）

## Acceptance Criteria

- HTTP 200 且 QC pass。
- 第 4 集 Script 与 `qc_approved` Memory 同时保存，QC approved_memory 与正式 Memory 完全一致。
- 第 3 集到期义务逐条存在 resolution；标为 carried_forward 的同 ID 义务按当前集/下一集号和本集来源路径写回，并有本集证据。
- 第 1–3 集 Script/Memory 和现有第 4 集 Brief 均未被覆盖。
- 若非 200，确认第 4 集正式 Script/Memory 均保持缺失并报告唯一失败阶段；不得修改代码、Prompt 或重试。

## Verification

```powershell
git diff --check
git diff --name-only -- app tests requirements.txt
```

完成后停止等待 Codex；无论成功失败都不得执行第 5 集。

