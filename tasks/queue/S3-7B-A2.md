# Task S3-7B-A2

`AUTO_CONTINUE: yes`

## Goal

按现有正式流程生成并审计项目 16 第 3 集。

## Authorization And Flow

- 明确授权真实 DeepSeek 调用以及现有 API 对 `data/app.db`、`logs/app.jsonl` 的正常写入。
- 若第 3 集 Brief 缺失，只调用一次 `POST /api/v1/projects/16/episodes/3/writer-brief`；已存在则直接复用。
- Brief 成功存在后，只调用一次第 3 集 script API，请求开启 Brief、Showrunner QC，并设置 `max_revision_attempts=2`。
- 每个请求使用唯一 `X-Request-ID`，调用前确认日志中不存在；不因非 200 重发。

## Allowed Files

- `data/app.db`、`logs/app.jsonl`（仅由现有 API 写入）
- `tasks/execution_log.md`（仅追加）

## Acceptance Gate

只有 script 请求 HTTP 200、QC pass、Script 与 `qc_approved` Memory 同时保存、QC approved_memory 等于正式 Memory、Brief 未被剧本请求覆盖、第 1–2 集未变化，才可继续 A3。否则记录状态、日志阶段与原子性结果后停止，不修改代码或 Prompt。

## Verification

```powershell
git diff --check
git diff --name-only -- app tests requirements.txt
```

