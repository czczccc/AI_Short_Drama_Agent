# Task S3-7B-A1

`AUTO_CONTINUE: yes`

## Goal

只读建立项目 16 第 3–5 集测试前基线，并确认第 2 集正式 Memory 的连续性义务足以生成第 3 集 Brief。

## Allowed Files

- `tasks/execution_log.md`（仅追加）

## Requirements

1. 以 SQLite 只读模式确认项目 16 的第 1–2 集 Script/Memory 均存在，第 2 集 Memory 为 `qc_approved`。
2. 记录第 2 集 `continuity_obligations` 数量、稳定 ID、到期集号和来源路径；不得记录完整剧情正文。
3. 记录第 3–5 集 Script、Memory、Brief、QC 的存在性及稳定 SHA-256；缺失记为 `absent`。
4. 确认 Episode Plan 覆盖第 3–5 集，数据库查询前后文件指纹不变，真实 Provider 配置可用且不泄密。

## Verification

```powershell
git diff --check
git diff --name-only -- app tests requirements.txt
```

前置数据完整且只读检查未写库才可继续 A2，否则停止批次。

