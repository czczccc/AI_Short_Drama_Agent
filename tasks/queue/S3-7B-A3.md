# Task S3-7B-A3

`AUTO_CONTINUE: yes`

## Goal

在第 3 集门禁通过后，按同一正式流程生成并审计项目 16 第 4 集。

## Authorization And Flow

- 授权真实 LLM 和现有 API 正常写库、写日志。
- Brief 缺失时只生成一次；已存在则复用。随后只发送一次开启 Brief、Showrunner QC、最多两次返修的第 4 集 script 请求。
- 请求分别使用唯一 Request ID，禁止为改变结果而重发。
- 记录安全日志链、状态码、耗时、LLM/Writer/QC 尝试数和生成前后稳定 SHA-256。

## Allowed Files

- `data/app.db`、`logs/app.jsonl`（仅由现有 API 写入）
- `tasks/execution_log.md`（仅追加）

## Acceptance Gate

只有 HTTP 200、QC pass、第 4 集 Script/Memory 原子保存、Memory 为 qc_approved、QC approved_memory 完全一致、Brief 和第 1–3 集不变，才继续 A4；否则停止且不修代码。

## Verification

```powershell
git diff --check
git diff --name-only -- app tests requirements.txt
```

