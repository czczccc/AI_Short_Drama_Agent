# Task S3-6.3-D

`AUTO_CONTINUE: yes`

## Goal

用聚焦单元测试证明 `complete_missing_continuity_obligations()` 保留已有合法义务及其证据，不为同一 `source_memory_path` 创建重复义务，并且重复调用幂等。

## Allowed Files

- `tests/test_qc_grounding.py`
- `tasks/execution_log.md`（仅追加）
- `app/services/qc_grounding.py`（仅测试揭示缺口时允许最小修改；一旦修改，完成本任务后必须停止批次）

## Requirements

1. 构造第1集 pass 报告，`unresolved_questions.0` 已有合法义务和 `continuity_obligations.0` 证据。
2. 调用补全函数，断言义务数量仍为1、原义务和证据完整不变、同来源路径不重复。
3. 对结果再次调用，断言完整 JSON 与第一次结果相同。
4. 只按精确 `source_memory_path` 判断，不增加模糊匹配。

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest -q tests/test_qc_grounding.py
.\.venv\Scripts\python.exe -m pytest -q tests/test_scripts.py::test_showrunner_qc_completes_missing_unresolved_question_obligation
git diff --check
```

按 `tasks/execution_log.md` 模板追加 `S3-6.3-D/attempt-N`。若修改了生产代码，即使测试通过也停止批次。
